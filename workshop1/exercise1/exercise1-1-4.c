#include <stdio.h>

int main() {
    int amount = 0;

    printf("Please insert the amount of integers to be stored in an array:\n");
    scanf("%d", &amount);
    int array[amount];
    for (int i = 0; i < amount; ++i) {
        
    }
}